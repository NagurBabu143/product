import React from "react";
import { Typography, Box } from "@mui/material";

const TypographySection = () => {
  return (
    <Box p={2}>
      <Typography variant="h4">Typography Example</Typography>
      <Typography variant="body1">This is a body text example.</Typography>
    </Box>
  );
};

export default TypographySection;
